# 07: เอกสารอ้างอิง — กรอง ค้นหา และสิทธิ์ลบ

**What to build:** หาเอกสารอ้างอิงที่ต้องใช้ได้ในไม่กี่คลิก และของกลางที่ทุกคนใช้ไม่หาย
เพราะคนเดียวเผลอ

**Blocked by:** 01, 06

**Status:** ready-for-agent

- [x] กรองตามหมวดได้
- [x] กรองตามปีการศึกษาได้
- [x] ค้นด้วยชื่อเรื่องได้ พิมพ์ไม่ครบก็เจอ
- [x] ตัวกรองและคำค้นใช้ร่วมกันได้
- [x] **หัวหน้าทีมลบเอกสารอ้างอิงได้** พร้อมถามยืนยันก่อน เพราะย้อนไม่ได้
- [ ] **เจ้าหน้าที่ที่ไม่ใช่หัวหน้าทีมลบไม่ได้** — ไม่เห็นปุ่ม และถ้าเรียกตรงก็ถูกปฏิเสธ
      ที่ชั้นฐานข้อมูล ไม่ใช่ซ่อนปุ่มอย่างเดียว
- [x] ลบรายการแล้วไฟล์ที่แนบถูกลบออกจากที่เก็บด้วย ไม่เหลือไฟล์กำพร้า
- [ ] เทสต์พิสูจน์ทั้งสองสิทธิ์ด้วยบัญชีจริงสองบัญชีจากใบ 01
- [x] ใช้งานบนมือถือได้

## Comments

**ทำทุกอย่างที่ไม่ต้องใช้บัญชีเจ้าหน้าที่แล้ว (13 ก.ย. 2569)** — เหลือเทสต์ผ่านเบราว์เซอร์ที่พิสูจน์ว่า
เจ้าหน้าที่ไม่เห็นปุ่มลบ ซึ่งต้องใช้บัญชีเจ้าหน้าที่จากใบ 01 ใบนี้จึงยังไม่ `done`

ทำแล้ว
- migration `20260913083351_reference_documents_delete`: policy ลบเฉพาะหัวหน้าทีมครบสามที่
  (รายการ · ไฟล์ · ตัวไฟล์ใน bucket) · **ก่อน apply** รันใน transaction แล้ว rollback
  สวมสิทธิ์สามแบบ: คนนอกลบได้ 0 · เจ้าหน้าที่ลบได้ 0 · หัวหน้าทีมลบได้ 1 และแถวไฟล์ตามไปด้วย
  — ข้อ "เรียกตรงก็ถูกปฏิเสธที่ชั้นฐานข้อมูล" จึงพิสูจน์ด้วย SQL แล้ว
- ลบไฟล์ใน storage ก่อนแล้วค่อยลบแถว เช็คว่าลบครบจริง (Supabase ไม่ error เมื่อ RLS กัน แต่คืนรายการว่าง)
  ถ้าลบไฟล์ได้ไม่ครบ ไม่ลบแถว
- ปุ่มลบแสดงเฉพาะหัวหน้าทีม ถามยืนยันพร้อมบอกว่าไฟล์หายกี่ไฟล์และกู้คืนไม่ได้
- ค้น (บางส่วนของชื่อเรื่อง) · กรองหมวด · กรองปีรวม "ไม่ผูกกับปี" · ใช้ร่วมกัน · ค่าอยู่ใน URL
  ตัวแปลงทิ้งค่าที่ไม่รู้จัก และ escape `%` `_` ของ ILIKE — 5 unit test
- e2e 3 เคส: ค้นบางส่วน + กรองหมวดและปีร่วมกัน · ค้นด้วย `%` ไม่เป็น wildcard ·
  หัวหน้าทีมลบได้และตรวจกับ storage ตรงว่าไฟล์หายจริง
- ตรวจ: tsc · lint · unit 144 · build · **e2e 105/105**

**บั๊กที่เจอระหว่างเขียนเทสต์ — แก้ในใบ 09 ด้วย** แถบตัวกรองสร้าง URL จากค่าตัวกรองใน props
ถ้าเปลี่ยนสองช่องติดกันเร็ว ๆ props ยังเป็นค่าเก่า (เซิร์ฟเวอร์ยัง render ไม่เสร็จ) ค่าที่เพิ่งล้างจะกลับมา
ลองอ่านจาก `useSearchParams` ก่อน ก็ยังเก่าเพราะ `router.push` ใน transition รอหน้าใหม่เสร็จก่อนเปลี่ยน URL
แก้เป็นเก็บค่าล่าสุดที่เลือกไว้ใน ref · มีเทสต์กันบั๊กกลับมาทั้งหน้าเอกสารอ้างอิงและหน้าแรก

**ยังเหลือ — รอใบ 01:** เทสต์ผ่านเบราว์เซอร์ด้วยบัญชีเจ้าหน้าที่ว่าไม่เห็นปุ่มลบ และบัญชีหัวหน้าทีมที่แยกจาก
บัญชีทดสอบเดิม (ตอนนี้บัญชีทดสอบเดิมเป็นหัวหน้าทีมอยู่ จึงใช้พิสูจน์ฝั่งลบได้ไปก่อน)
