# 04: แทนข้อมูลจริงด้วยข้อมูลสมมติ

**What to build:** ถอดข้อมูลส่วนบุคคลจริงออกจากระบบทั้งหมดแล้วแทนด้วยข้อมูลสมมติ
โดยยังนำข้อมูลจริงกลับเข้ามาได้เมื่อพร้อมใช้งานจริง

**⚠️ ใบนี้ลบข้อมูลจริง** ก่อนเริ่มต้องรายงานจำนวนแถวที่จะกระทบให้คนอนุมัติเห็นก่อน
และต้องยืนยันว่าไฟล์นำเข้าต้นฉบับถูกสำรองไว้แล้ว เพราะหลังล้างฐานข้อมูล
ไฟล์นั้นจะเป็นสำเนาเดียวของข้อมูลจริงที่เหลืออยู่

ต้องล้างแล้วนำเข้าใหม่ **ไม่ใช่เขียนทับ payload ของเหตุการณ์เดิม** เพราะ ADR-0001
และ `CONTEXT.md` กำหนดว่าเหตุการณ์บันทึกแล้วไม่ลบไม่แก้ การแทนข้อมูลจึงต้องเป็น
ชุดเหตุการณ์ใหม่ทั้งชุด และการแก้แค่ตารางสถานะปัจจุบันจะถูกลบล้างทันทีที่มีคนสั่งสร้างใหม่จากเหตุการณ์

**Blocked by:** 02, 03

**Status:** done

- [x] รายงานจำนวนแถวและไฟล์ที่จะถูกลบให้เห็นก่อนลงมือ
- [x] ล้างคนและเหตุการณ์ พร้อมของที่อ้างถึงคน (การสมัคร การชำระเงิน เอกสาร คำขอศูนย์สอบ
      ร่องรอยการเปิดดู รายการที่ถูกรวม)
- [x] นำเข้าใหม่จากไฟล์สมมติของใบ 03 ได้จำนวนแถวเท่าไฟล์จริง
- [x] ลบไฟล์ทั้งหมดในที่เก็บเอกสารรายคน — มีสำเนาบัตรประชาชนจริงอยู่ในนั้น
      ถ้าไม่ลบ การถอดข้อมูลจริงออกจะทำแค่ครึ่งเดียว
- [x] ชื่อเจ้าหน้าที่จริงถูกแทนด้วยชื่อสมมติ **ทั้งในฐานข้อมูลและในโค้ด**: ตารางแปลงชื่อผู้ดูแล
      ตอนนำเข้า · เทสต์ของตารางนั้น · เทสต์กระดานงานค้างที่ยืนยันว่าชื่อจริงไม่โผล่ ·
      เอกสารออกแบบที่อ้างชื่อคนจริง
- [x] ตรวจคลังคำตอบทีละรายการ แทนชื่อคนจริงถ้าพบ
- [x] ค้นทั้งที่เก็บโค้ดยืนยันว่าไม่มีชื่อคนจริงเหลืออยู่ที่ใด
- [x] สั่งสร้างสถานะปัจจุบันใหม่จากเหตุการณ์แล้วตรงทุกแถว
- [x] ไม่มีรายการที่วันติดต่อเข้ามาอยู่ในอนาคตเหลืออยู่
- [x] เทสต์ทั้งหมดยังผ่านครบ
- [x] เขียนขั้นตอนนำข้อมูลจริงกลับเข้าระบบไว้ในเอกสารความคืบหน้า

## Comments

**จากใบ 03** — ไฟล์สมมติอยู่ที่ `data/leads-mock.csv` ใช้ชื่อเจ้าหน้าที่สมมติชุดในตาราง
ของ `data/README.md` ต้องใช้ชุดเดียวกันตอนแทนชื่อในฐานข้อมูล ไม่งั้นตารางแปลงชื่อผู้ดูแล
จะจับคู่ไม่ได้ · และมีเรื่องค้างตัดสิน: ค่าเผื่อ 30 วันของด่านวันที่ทำให้ 9 แถวยังอยู่ในอนาคต
ซึ่งขัดกับข้อ acceptance ของใบนี้ (รายละเอียดในใบ 03)

**จาก /code-review ของใบ 03** — ตัวสร้างไฟล์สมมติไม่มีชื่อเจ้าหน้าที่จริงแล้ว (ย้ายไปไฟล์ local)
แต่ชื่อจริงยังค้างใน git history ที่ commit e3fec50 และที่ใบนี้ต้องแทนอยู่แล้ว
(`normalize.ts` คอมเมนต์หัวไฟล์ · `normalize.test.ts` · `e2e/work-board.spec.ts` · `docs/PROMPT-*.md`)
ถ้าจะให้ "ค้นทั้งที่เก็บโค้ดยืนยันว่าไม่มีชื่อคนจริงเหลืออยู่" ครอบ history ด้วย ต้องขออนุมัติ rewrite history

## ผลการทำ (13 ก.ย. 2569)

หัวหน้าทีมให้ agent ตัดสินข้อค้างทั้งหมดแทน ("คำถามที่เหลือเลือกให้เลย") — ตัดสินไว้ดังนี้

- **ด่านวันติดต่อเข้ามา: ไม่มีค่าเผื่อ** ต้องไม่เลยวันนี้ตามเวลาไทย (เดิม 30 วัน ปล่อย 9 แถวผ่าน)
  ไฟล์สมมติจึงทิ้งวันที่ 26 แถว
- **แถวที่ไม่มีวันติดต่อเข้ามา (ว่าง/ทิ้ง): ใช้วันของแถวก่อนหน้าในชีท** เพราะชีทกรอกต่อท้ายตามเวลา
  (ถอยหลังเกิน 7 วันแค่ 12 จาก 3,110 แถว) · แถวต้นไฟล์ที่ไม่มีแถวก่อนหน้า ใช้วันติดตามที่เก่าสุด
  ไม่มีก็ใช้วันแรกของไฟล์ · เลิกใช้เวลานำเข้า ซึ่งเคยทำให้กรวยเดือนที่นำเข้าบวม
  ผล: แถวก่อนหน้า 65 · วันติดตาม 8 · วันแรกของไฟล์ 3
- **สำรองก่อนลบ** agent ทำเอง ที่ `~/btu-admin-backup-20260913/` (นอก repo นอกโฟลเดอร์ซิงก์ สิทธิ์ 700)
  ชีทต้นฉบับ CSV (sha256 ตรงกับต้นทาง) + xlsx · ทุกตาราง public เป็น JSON · ไฟล์ Storage ทั้งสอง bucket
  ด้วยสคริปต์ใหม่ `npm run db:backup` — จำนวนตรงกับฐานข้อมูลทุกตาราง
- **ถ้อยคำสมมติในหมายเหตุ/ช่องติดตาม** ยอมรับ · **ไม่ rewrite git history** (e3fec50 ยังมีชื่อจริง —
  ย้อนยาก ต้องให้หัวหน้าทีมสั่งเองถ้าจะทำ)

**จำนวนที่ลบ** (`npm run db:reset-to-mock` นับก่อน แล้ว `--confirm`): people 3,922 · events 7,688 ·
applications 159 · payments 87 · documents 180 · document_access_log 94 · exam_center_requests 51 ·
merged_people 22 · ไฟล์เอกสารรายคน 273 · แถวเจ้าหน้าที่เปลี่ยนเป็นชื่อสมมติ 5 จาก 7

**นำเข้าใหม่** 3,177 ราย · 5,892 เหตุการณ์ · จำนวนเคสของเจ้าหน้าที่แต่ละคนเท่าเดิมทุกคน
(613/622/477/381/2 — ตารางแปลงชื่อจับคู่ครบ) · `db:verify` ตรงทุกแถว · วันติดต่อในอนาคต 0

**เจอระหว่างทำ — service role เรียก `record_event()` ไม่ได้มาตั้งแต่ 11 ก.ย.** เพราะฟังก์ชันอ่าน
`private.current_staff_id()` แต่ service_role ไม่มีสิทธิ์ใน schema `private` (นำเข้ารอบก่อนใช้บัญชีทดสอบ
จึงไม่เคยเห็น) · migration `20260913144156` ให้สิทธิ์เฉพาะสองฟังก์ชันหาเจ้าหน้าที่ `national_id_key` ยังล็อก

**ชื่อจริงในโค้ด** แทนด้วยชุดสมมติ: เทสต์วรรณยุกต์ซ้ำ · คอมเมนต์ `normalize.ts` · `docs/PROMPT-*.md` ·
ใบ 03 · เทสต์กระดานงานค้างเปลี่ยนเป็นอ่านชื่อจากฐานข้อมูล (แบบเดียวกับ `home.spec.ts`) ·
`git grep` ชื่อจริงและอีเมลบัญชีเดิมทั้ง repo ไม่เจอ · คลังคำตอบ 44 รายการและเอกสารอ้างอิงไม่มีชื่อจริง

**เทสต์ไม่นิ่งตอนเครื่องมีโหลด** (load avg ~5 จากโปรเจกต์อื่น) — ไล่แล้วพบบั๊กจริงหนึ่งข้อ:
อัปโหลดเอกสารเสร็จแล้วปลดสถานะ "กำลังอัปโหลด" ก่อนหน้าโหลดข้อมูลใหม่ แถวจึงเด้งกลับเป็น "ยังไม่ส่ง"
ชั่วครู่ · แก้ให้ปลดใน transition เดียวกับ `router.refresh()` · ด่านที่รออัปโหลดเสร็จใช้ `UPLOAD_DONE` 20 วินาที

ตรวจ: tsc · lint · unit 146 · build · **e2e 133/133** (รวมเทสต์ใหม่ของใบ 05) · db:verify

ขั้นตอนนำข้อมูลจริงกลับอยู่ใน `docs/PROGRESS.md` หัวข้อ "นำข้อมูลจริงกลับเข้าระบบ"
