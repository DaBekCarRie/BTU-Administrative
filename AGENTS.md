# AGENTS.md

ระบบจัดการผู้สนใจและผู้เรียน ทีม LMS มหาวิทยาลัยกรุงเทพธนบุรี · ภาษาไทยล้วน · ผู้ใช้ 6 คน

**อ่าน [`CLAUDE.md`](./CLAUDE.md) ก่อนเขียนโค้ด** — มีกฎเหล็ก 7 ข้อที่ห้ามฝ่าฝืน
ไฟล์นี้เป็นสรุปสำหรับ agent ที่เพิ่งเข้ามา ไม่ทวนสิ่งที่เอกสารอื่นเขียนไว้แล้ว

## เริ่มที่ไหน

งานรอบปัจจุบันอยู่ที่ [`.scratch/lockdown-and-extensions/`](./.scratch/lockdown-and-extensions/)
อ่าน `spec.md` ก่อน แล้วหยิบใบงานจาก `issues/`

**ใบที่หยิบได้ทันที (blocker ครบแล้ว): `01`** — ต้องตั้ง `SUPABASE_SERVICE_ROLE_KEY` ใน `.env.local` ก่อน

`03` เสร็จแล้ว (ดูผลและเรื่องค้างตัดสินในใบ)

| ใบ | เรื่อง | รอใบ |
| --- | --- | --- |
| 01 | ตัวตั้งต้นการล็อกอินสามสิทธิ์ | — |
| 02 | 🟡 ปิดสิทธิ์เฉพาะเจ้าหน้าที่ — แก้แล้ว เหลือเทสต์คนนอก | 01 |
| 03 | ✅ เตรียมข้อมูลสมมติ (ด่านวันที่ + ไฟล์นำเข้า) | — |
| 04 | ⚠️ แทนข้อมูลจริงด้วยข้อมูลสมมติ | 02, 03 |
| 05 | จัดบัญชีและปิดการสมัครใหม่ | 02, 04 |
| 06 | เอกสารอ้างอิง — เก็บและเปิดดู | 02 |
| 07 | เอกสารอ้างอิง — กรอง ค้นหา สิทธิ์ลบ | 01, 06 |
| 08 | ✅ หน้าแรก — กรวยเดือนนี้เทียบเดือนที่แล้ว | 02 |
| 09 | ✅ หน้าแรก — ตัวกรอง + ย้อนหลัง 6 เดือน | 08 |
| 10 | ✅ โต๊ะตรวจเอกสารต้องเห็นคนที่ส่งเอกสารครบทุกคน | 02 |

ก่อนเริ่มงานใบใด ตั้ง `Status: claimed` ในไฟล์ใบนั้นแล้วบันทึกก่อน

## เรื่องด่วนที่ต้องรู้

**ช่องโหว่สิทธิ์ปิดแล้วที่ฐานข้อมูล (13 ก.ย. 2569)** — policy ทุกตัวเป็น "เจ้าหน้าที่ที่ยังใช้งานอยู่" แล้ว
(ADR-0005) แต่ใบ 02 ยังเหลือเทสต์อัตโนมัติของคนนอกที่รอบัญชีจากใบ 01
และ `disable_signup` ยังเป็นเท็จ — หัวหน้าทีมควรปิดบน Dashboard ด้วย

**ข้อมูลในฐานเป็นข้อมูลจริงของคนจริง** จนกว่าใบ 04 จะเสร็จ ระวังทุกคำสั่งที่ลบหรือแก้ข้อมูล

## กฎที่ห้ามฝ่าฝืน (นอกเหนือจาก CLAUDE.md)

- **เหตุการณ์บันทึกแล้วไม่ลบไม่แก้** (ADR-0001) — ตาราง `people` เป็นแค่สถานะปัจจุบันที่
  คำนวณจากเหตุการณ์ ถ้าแก้แค่ `people` คำสั่งสร้างใหม่จากเหตุการณ์จะลบล้างทันที
  การเขียนข้อมูลคนต้องผ่านทางเดียวคือ `recordEvent()`
- **ห้ามกั้นข้อมูลระหว่างเจ้าหน้าที่** (ADR-0004) — เจ้าหน้าที่ทุกคนเห็นทุกอย่าง
  งานรอบนี้กั้นเฉพาะเส้นแบ่ง "เจ้าหน้าที่" กับ "คนนอกทีม" ซึ่งเป็นคนละเรื่อง
- **ห้ามใส่กราฟหรือแผนภูมิ** — อยู่ใน Out of Scope และมีเทสต์คุมอยู่
- **ห้ามแสดงตัวเลขแยกรายบุคคลของเจ้าหน้าที่** — มีเทสต์คุมอยู่
- **ห้ามใช้คำว่า "แอดมิน" หรือ "admin" ในข้อความภาษาไทย** — `CONTEXT.md` สั่งเลี่ยง
  เพราะกำกวมกับ `role` ของระบบ ใช้ **เจ้าหน้าที่** และ **หัวหน้าทีม**
- **route handler ทุกตัวต้องมีด่านตรวจสิทธิ์ของตัวเอง** — มันไม่วิ่งผ่าน layout
  (เป็นต้นเหตุของช่องโหว่ที่หน้าส่งออกไฟล์) — ตอนนี้เป็นกฎข้อ 8 ใน CLAUDE.md แล้ว
- ศัพท์ทุกคำใช้ตาม [`CONTEXT.md`](./CONTEXT.md) ซึ่งเป็นคำที่ทีมใช้จริง

## คำสั่งที่ใช้ตรวจงาน

```bash
npm run typecheck      # ด่านหลัก ต้องผ่านก่อนบอกว่าเสร็จ
npm run test           # unit 130 เทสต์
npm run lint
npm run build
npx playwright test    # e2e 91 เทสต์
npm run db:verify      # เทียบว่าสถานะปัจจุบันตรงกับที่เล่นใหม่จากเหตุการณ์
```

**อย่าเชื่อว่าเสร็จจนรันครบ** และอย่ารายงานว่าผ่านถ้าไม่ได้รันเอง

## กับดักของสภาพแวดล้อมนี้

- **e2e รันกับฐานข้อมูลจริง** และทิ้งแถวทดสอบไว้ทุกรอบ (สะสมแล้ว 223 แถว)
  ชื่อแถวทดสอบลงท้ายด้วยขีดกลางและอักษรสุ่มเจ็ดตัว เวลาจะล้างต้องเทียบ pattern
  กับชื่อจริงก่อนทุกครั้ง เคยมี regex ไปโดนชื่อคนจริงมาแล้ว
- **e2e ใช้พอร์ต 3100 และรัน production build** ปิด `npm run dev` ก่อน
  และอย่ารัน `npm run build` ซ้อนกับ playwright เพราะแย่ง `.next/`
- **ลบ route แล้ว `typecheck` อาจฟ้องไฟล์ที่ Next สร้างไว้** ไม่ใช่ความผิดของ source
  ล้างด้วยการลบ `.next/types` กับ `.next/dev/types` แล้ว build ใหม่
- **แก้ schema ต้องเขียนไฟล์ migration** และชื่อไฟล์ต้องตรงกับ version ที่ฐานข้อมูลบันทึก
  ตอนนี้ 27 ไฟล์ = 27 version ตรงกันหมด ห้ามให้หลุด
- **`npm run db:types` ใช้ได้แล้ว** หลังจาก `supabase link` แต่ถ้าพังมันจะไม่ลบไฟล์ types ทิ้ง
  (เคยลบมาแล้ว จึงแก้ให้เขียนลงไฟล์ชั่วคราวก่อน)
- **โมดูลโดเมนห้าม import อะไรที่แตะ I/O** — `import type` ไม่นับเพราะถูกลบตอนคอมไพล์

## บทเรียนจากการตรวจงานรอบก่อน

รายงานของ agent ที่มาตรวจโค้ด **ผิด 2 ข้อจาก 17 ข้อ** ทั้งสองข้อดูข้อมูลต้นทางหรือ spec
แล้วรู้ทันทีว่าไม่ใช่บั๊ก — **เปิดโค้ดและข้อมูลยืนยันทุกข้อก่อนลงมือแก้** และก่อนลบอะไร
ที่ถูกบอกว่า "ไม่มีใครเรียก" ให้ค้นทั้งที่เก็บโค้ดยืนยันเองก่อน

รายละเอียดว่าอะไรถูกซ่อมไปแล้ว อะไรที่ทักมาแต่ไม่ใช่บั๊ก และอะไรที่หัวหน้าทีมต้องทำเอง
อยู่ใน [`docs/PROGRESS.md`](./docs/PROGRESS.md)

บริบทของเซสชันที่ส่งมอบงานรอบนี้ — รวมสิ่งที่หัวหน้าทีมต้องทำเองก่อน agent เริ่มได้
และข้อเท็จจริงที่ค้นมาแล้วไม่ต้องค้นซ้ำ — อยู่ใน [`docs/HANDOFF.md`](./docs/HANDOFF.md)
